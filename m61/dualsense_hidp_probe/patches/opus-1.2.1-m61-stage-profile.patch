diff --git a/celt/celt_encoder.c b/celt/celt_encoder.c
--- a/celt/celt_encoder.c
+++ b/celt/celt_encoder.c
@@ -49,6 +49,13 @@
 #include "celt_lpc.h"
 #include "vq.h"
 
+#if defined(M61_OPUS_STAGE_PROFILE)
+extern void m61_opus_stage_mark(unsigned boundary);
+# define M61_OPUS_STAGE_MARK(boundary) m61_opus_stage_mark(boundary)
+#else
+# define M61_OPUS_STAGE_MARK(boundary) do { (void)(boundary); } while (0)
+#endif
+
 
 /** Encoder state
  @brief Encoder state
@@ -1451,6 +1459,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
    VARDECL(opus_val16, surround_dynalloc);
    ALLOC_STACK;
 
+   M61_OPUS_STAGE_MARK(0);
    mode = st->mode;
    nbEBands = mode->nbEBands;
    overlap = mode->overlap;
@@ -1616,5 +1625,6 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
       enc->nbits_total+=tell-ec_tell(enc);
    }
+   M61_OPUS_STAGE_MARK(1);
    c=0; do {
       int need_clip=0;
 #ifndef FIXED_POINT
@@ -1678,6 +1688,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
       transient_got_disabled=1;
    }
 
+   M61_OPUS_STAGE_MARK(2);
    ALLOC(freq, CC*N, celt_sig); /**< Interleaved signal MDCTs */
    ALLOC(bandE,nbEBands*CC, celt_ener);
    ALLOC(bandLogE,nbEBands*CC, opus_val16);
@@ -1865,6 +1876,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
       tf_select=0;
    }
 
+   M61_OPUS_STAGE_MARK(3);
    ALLOC(error, C*nbEBands, opus_val16);
    c=0;
    do {
@@ -1934,6 +1946,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
       ec_enc_icdf(enc, st->spread_decision, spread_icdf, 5);
    }
 
+   M61_OPUS_STAGE_MARK(4);
    ALLOC(offsets, nbEBands, int);
 
    maxDepth = dynalloc_analysis(bandLogE, bandLogE2, nbEBands, start, end, C, offsets,
@@ -2162,8 +2175,9 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
    else
       st->lastCodedBands = codedBands;
 
+   M61_OPUS_STAGE_MARK(5);
    quant_fine_energy(mode, start, end, oldBandE, error, fine_quant, enc, C);
 
    /* Residual quantisation */
    ALLOC(collapse_masks, C*nbEBands, unsigned char);
    quant_all_bands(1, mode, start, end, X, C==2 ? X+N : NULL, collapse_masks,
@@ -2175,6 +2190,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
          dual_stereo, st->intensity, tf_res, nbCompressedBytes*(8<<BITRES)-anti_collapse_rsv,
          balance, enc, LM, codedBands, &st->rng, st->complexity, st->arch, st->disable_inv);
 
+   M61_OPUS_STAGE_MARK(6);
    if (anti_collapse_rsv > 0)
    {
       anti_collapse_on = st->consec_transient<2;
@@ -2285,6 +2301,7 @@ int celt_encode_with_ec(CELTEncoder * OPUS_RESTRICT st, const opus_val16 * pcm,
    /* If there's any room left (can only happen for very high rates),
       it's already filled with zeros */
    ec_enc_done(enc);
+   M61_OPUS_STAGE_MARK(7);
 
 #ifdef CUSTOM_MODES
    if (st->signalling)
