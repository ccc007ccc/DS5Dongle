diff --git a/celt/entcode.h b/celt/entcode.h
--- a/celt/entcode.h
+++ b/celt/entcode.h
@@ -152 +152,10 @@ static OPUS_INLINE opus_int32 celt_sudiv(opus_int32 n, opus_int32 d) {
-#endif
+#if defined(M61_OPUS_E907_POW2_DIV)
+/* Exact unsigned division for the CELT/PVQ block-stride invariant. */
+static OPUS_INLINE opus_uint32 celt_udiv_pow2(opus_uint32 n, opus_uint32 d) {
+   celt_assert(d>0);
+   celt_assert((d&(d-1))==0);
+   return n >> (EC_ILOG(d)-1);
+}
+#endif
+
+#endif
diff --git a/celt/vq.c b/celt/vq.c
--- a/celt/vq.c
+++ b/celt/vq.c
@@ -96,7 +96,11 @@ void exp_rotation(celt_norm *X, int len, int dir, int stride, int K, int spread)
    }
    /*NOTE: As a minor optimization, we could be passing around log2(B), not B, for both this and for
       extract_collapse_mask().*/
+#if defined(M61_OPUS_E907_POW2_DIV)
+   len = celt_udiv_pow2(len, stride);
+#else
    len = celt_udiv(len, stride);
+#endif
    for (i=0;i<stride;i++)
    {
       if (dir < 0)
@@ -145,7 +149,11 @@ static unsigned extract_collapse_mask(int *iy, int N, int B)
       return 1;
    /*NOTE: As a minor optimization, we could be passing around log2(B), not B, for both this and for
       exp_rotation().*/
+#if defined(M61_OPUS_E907_POW2_DIV)
+   N0 = celt_udiv_pow2(N, B);
+#else
    N0 = celt_udiv(N, B);
+#endif
    collapse_mask = 0;
    i=0; do {
       int j;
diff --git a/celt/bands.c b/celt/bands.c
--- a/celt/bands.c
+++ b/celt/bands.c
@@ -1138 +1138,5 @@ static unsigned quant_band(struct band_ctx *ctx, celt_norm *X,
-   N_B = celt_udiv(N_B, B);
+#if defined(M61_OPUS_E907_POW2_DIV)
+   N_B = celt_udiv_pow2(N_B, B);
+#else
+   N_B = celt_udiv(N_B, B);
+#endif
